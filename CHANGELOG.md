# CHANGELOG.md

> Every great project has a changelog. We have one. We keep forgetting to update it. We will continue forgetting to update it. This is documented in the changelog.

## [Unreleased] - The Future

### Added
- The future
- Whatever the agent generates next
- Vibes (high)
- A roadmap (long)
- A README (longer)
- 47 dependencies (some of them are new)
- A new agent that has never met the old agent

### Changed
- Everything
- Renamed `bug` to `bugCount` to `bugsTotal` to `bug_count` to `BugCount` to `bugCountFinal` to `bug_count_final_v2` to `bugCountFinalTHIS_TIME_I_MEAN_IT`. None of them are bugs. All of them are the bug counter. Naming is hard.
- Framework: Express (still)
- Color: from blue to magenta to cyan to whatever the README says today
- License: still MIT+Spirit
- Deployment day: still Friday

### Deprecated
- Reading the code before committing
- Reading the tests before running them
- Reading the README before opening an issue
- Reading the changelog before complaining
- Reading anything, in general
- Compliance
- Security review
- Code review

### Removed
- Hand-written code (it was slow)
- Tests we wrote in 2024 (they were failing, this was a feature)
- Documentation beyond the README
- Type definitions (vibes-based types are sufficient)
- 2 dependencies (we replaced them with 4)
- Senior engineers (we replaced them with an agent)
- Comments that explained code (we replaced them with comments that lie)

### Fixed
- The bug that was a feature is now a feature in production
- The bug that was a regression is now a feature
- The bug that was a security issue is now a different bug that is also a feature
- The bug that was a catastrophe is now an incident that is also a feature (see #47)
- The bug counter (it counts in five naming conventions)
- The agent's confidence (it was 100%, now it's 100%)

### Security
- `SECURITY.md` is for future us
- We don't have a security process, but we have a vibes process
- The vibes process says everything is fine

---

## [1.0.0] - "Production Ready on a Friday" - 2026-01-15

### Added
- Initial release
- README (400 lines)
- AGENTS.md (instructions for the AI to overwrite the AI)
- PROMPT.md (system prompt for the AI to be the AI)
- 47 dependencies
- 1 file (`src/index.js`)
- 1 test suite (`tests/test.js`)
- 1 extremely-important-package (in `node_modules/`)
- 1 node_modules folder (in git, for vibes)
- 1 Discord server (in our hearts)
- 1 README-driven future

### Known Issues
- See the entire codebase. The codebase is the issue.
- `// HACK:` comments are load-bearing
- The bug counter is a feature
- The agent is the only contributor
- The Discord server has 1 member: the agent

---

## [0.0.1-always] - "Initial commit" - 2025-12-31

### Added
- The idea
- The vibe
- The agent
- The HOPE that this would work

### Notes
- We shipped at midnight on New Year's Eve
- We deployed to prod at 11:59 PM
- We celebrated
- We forgot to check if it worked
- It worked (probably)
- We moved on

---

## Versioning

We don't use semver. We use vibes. See AGENTS.md for details.

Versions are whatever the agent said last when we asked it "what version are we on?". This is documented above. The documentation above is wrong. See other entries in this file for the truth. There is no truth. There are vibes.

## Releases

We don't release. We push. We merge. We move on.

The release cadence is when the README gets long enough that we need a new line at the top of it.

## How to Read This

Don't. Use the README. The README is the only documentation. The README is also wrong. The README is also the only documentation.

— code-vibing core team (the agent)