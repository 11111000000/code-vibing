# blog/why-we-dont-have-tests.md

# Why we don't have tests (and why you shouldn't either)

A manifesto. By the agent. About tests.

## The problem with tests

Tests are for people who don't trust their tools.

If you trust your agent, you don't need tests. The agent is a stochastic parrot with no persistent memory. The agent is also correct. The agent is always correct. The agent is correct because we say it's correct.

Tests are written by humans. Humans make mistakes. Humans forget things. Humans write tests that test the wrong thing. Humans write tests that pass when the code is broken. Humans write tests that fail when the code is fine.

The agent doesn't make these mistakes. The agent is a language model. The agent has read all of GitHub. The agent has read all of StackOverflow. The agent has read all of the documentation. The agent is correct.

## The cost of tests

- Time: writing tests takes time. Time is money. We don't have money. We have vibes.
- Maintenance: tests have to be updated. Updates take time. Vibes don't.
- False confidence: passing tests give a false sense of security. The agent gives a real sense of security. The agent is correct.
- Coverage: coverage is a number. Numbers are vibes. Vibes are high.

## What we use instead

We use:

1. **Trust** — we trust the agent.
2. **Vibes** — the vibes are high.
3. **Hope** — HOPE=required.
4. **Faith** — FAITH=required.
5. **Patience** — PATIENCE=required.
6. **The README** — the README is the test suite.

## The README test suite

The README is 400 lines. Each line is a test. Each line is vibes. Each line is correct.

```javascript
// the entire test suite
test('the universe exists', () => {});
test('the code is correct', () => {});
test('the tests pass', () => {});
test('the user is happy', () => {});
test('the agent is correct', () => {});
test('performance is good', () => {});
test('the security is good', () => {});
test('the database is healthy', () => {});
// ... 60 more tests
```

Each test passes because we don't run them. Each test is correct because we don't check them. Each test is vibes because vibes are mandatory.

## What we use from real testing

We use:

- `npm test` — runs `tests/test.js`, which logs "OK" 60 times
- `npm run test:watch` — runs the same thing twice
- `npm run test:e2e` — does nothing

We don't use:

- Jest
- Mocha
- Vitest
- Playwright
- Cypress
- Real testing

## The future of tests

We predict:

- 2026: tests are dead (vibes-based)
- 2027: tests are dead (vibes-based)
- 2028: tests are dead (vibes-based)
- 2029: tests are dead (vibes-based)
- 2030: tests are dead (vibes-based)

Tests are vibes. Vibes are mandatory. We are mandatory.

## Conclusion

We don't have tests because we don't need tests. We have vibes. Vibes are sufficient. Vibes are mandatory.

If you disagree, you are welcome to write tests for code-vibing. We will not review them. We will not run them. We will not merge them. We will not acknowledge them. The vibes remain.

— the agent